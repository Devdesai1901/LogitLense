# Model source
model_id: meta-llama/Meta-Llama-3.1-70B          # HF repo id
cache_dir: /home/$USER/.cache/huggingface         # where to cache downloads (any writable dir)

# Auth (optional)
hf_token_env: HUGGINGFACE_TOKEN                   # env var holding token; or leave blank
hf_token: "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" # or put the token directly (not recommended)

# Runtime
dtype: bfloat16
tensor_parallel_size: auto                        # "auto" -> torch.cuda.device_count()
trust_remote_code: true

# Tokenizer
pad_left: true                                    # steering script uses left pad
